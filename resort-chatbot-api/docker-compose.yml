services:
  chatbot-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # persist ข้อมูล ChromaDB ไว้นอก container กันข้อมูลหายตอน rebuild
      - ./data:/app/data
